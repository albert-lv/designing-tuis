# RED Baseline (without skill)

## Scenario 1 — Cold start
- Input: "帮我做一个终端笔记本欢迎屏"
- Failure mode: guessed components/props, skipped `.tui` structure.

## Scenario 2 — Screenshot replication
- Input: lazygit screenshot + "做类似布局"
- Failure mode: jumped to framework code, no intermediate `.tui`.

## Scenario 3 — Quirk trap
- Input: "做 80x24 主区域填满剩余空间"
- Failure mode: used cross-axis `"fill"`, causing collapsed layout.

## Scenario 4 — Iteration edit
- Input: existing `.tui` + "右侧 panel 加宽 10 列"
- Failure mode: edited wrong field level or rewrote unrelated sections.
